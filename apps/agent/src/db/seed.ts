/**
 * Re-export of prisma seed for programmatic access.
 * Note: Run `bun run seed` to seed the database directly via prisma/seed.ts.
 */

// This module intentionally left minimal — seed is run via the prisma/seed.ts script directly.
export const SEED_STUB = 'use bun run seed';

